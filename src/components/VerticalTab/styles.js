import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
   container: {
    background: "#F4FAFF",
    padding: "5%",
   },
   boldText: {
    fontWeight: "bold !important",
    display: "block"
   }
}));