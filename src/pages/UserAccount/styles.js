import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
   container: {
    background: "#F4FAFF",
    padding: "0 5% 100px",
   },
   heading: {
    fontWeight: "bold !important",
    padding : "30px 0"
   }
}));